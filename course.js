const courseCatalog=window.JULI_COURSE_STRUCTURE||{};
const level=Number(document.body.dataset.courseLevel);const course=courseCatalog[level];const configured=window.JULI_SUPABASE_URL&&!window.JULI_SUPABASE_URL.startsWith('YOUR_')&&window.JULI_SUPABASE_ANON_KEY&&!window.JULI_SUPABASE_ANON_KEY.startsWith('YOUR_');const db=configured?window.supabase.createClient(window.JULI_SUPABASE_URL,window.JULI_SUPABASE_ANON_KEY):null;const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));let currentProfile=null,completed=new Set();
function blockKind(text){const t=text.trim().toLowerCase();if(t.startsWith('практика')||t.startsWith('задание'))return['Практика','practice'];if(t.startsWith('важно')||t.startsWith('обратите внимание'))return['Важно','important'];if(t.startsWith('цель'))return['Что вы изучите',''];if(t.startsWith('алгоритм')||t.startsWith('последовательность'))return['Алгоритм работы',''];if(t.startsWith('основн')||t.startsWith('ключев'))return['Ключевые моменты',''];return['Теория','']}
function lessonHtml(content){const parts=String(content||'').split(/\n\s*\n/).map(x=>x.trim()).filter(Boolean);return parts.map(p=>{const [label,cls]=blockKind(p);return `<div class="lesson-block ${cls}"><strong>${label}</strong><p>${esc(p)}</p></div>`}).join('')}
async function init(){if(!course){lock('Структура курса не загружена','Обновите страницу.');return}document.title=`${course.title} — JULI`;courseTitle.textContent=course.title;courseSubtitle.textContent=course.subtitle;courseLevel.textContent=`Ступень ${level}`;if(!db){lock('Система авторизации не настроена','Сначала необходимо подключить Supabase.');return}const {data:{user}}=await db.auth.getUser();if(!user){location.href='login.html';return}const {data:profile,error:pErr}=await db.from('profiles').select('*').eq('id',user.id).single();if(pErr||!profile){lock('Профиль не найден','Войдите в аккаунт повторно или обратитесь к преподавателю.');return}currentProfile=profile;if(profile.role!=='owner'&&((Object.hasOwn(profile,'is_active')&&!profile.is_active)||(Object.hasOwn(profile,'archived')&&profile.archived))){await db.auth.signOut();location.href='login.html?blocked=1';return}let allowed=profile.role==='owner';if(!allowed){const {data:access,error:aErr}=await db.from('course_access').select('access_granted').eq('student_id',user.id).eq('course_level',level).maybeSingle();if(aErr){lock('Не удалось проверить доступ','Обновите страницу или обратитесь к преподавателю.');return}allowed=!!access?.access_granted}if(!allowed){lock('Доступ к этой ступени закрыт','Преподаватель ещё не разрешил вам доступ к этому курсу.');return}const queries=[db.from('course_materials').select('module_no,title,content,sort_order').eq('course_level',level).eq('published',true).order('sort_order').order('module_no')];if(profile.role!=='owner')queries.push(db.from('student_progress').select('module_no,completed').eq('student_id',user.id).eq('course_level',level));const results=await Promise.all(queries);const materials=results[0].data,mErr=results[0].error;if(mErr){lock('Материалы временно недоступны','Не удалось загрузить содержание курса. Попробуйте позже.');return}if(results[1]?.error)console.warn('Progress error:',results[1].error.message);completed=new Set((results[1]?.data||[]).filter(x=>x.completed).map(x=>x.module_no));app.classList.remove('hidden');renderModules(materials||[]);notice.textContent=profile.role==='owner'?'Режим преподавателя: просмотр опубликованной версии курса.':'Материалы загружены из защищённой базы. Завершение модулей подтверждает преподаватель.';setTimeout(scrollToHash,50)}
function renderModules(materials){const byModule=new Map();materials.forEach(x=>{if(!byModule.has(x.module_no))byModule.set(x.module_no,[]);byModule.get(x.module_no).push(x)});modules.innerHTML=course.modules.map((def,i)=>{const n=i+1,rows=byModule.get(n)||[],title=rows[0]?.title||def.title,content=rows.map(r=>r.content).filter(Boolean).join('\n\n'),done=completed.has(n),owner=currentProfile?.role==='owner';return `<article class="module ${done?'completed':''}" id="module-${n}"><div class="eyebrow">Модуль ${n}</div><h2>${esc(title)}</h2>${content?`<div class="lesson">${lessonHtml(content)}</div>`:`<div class="module-empty">Материал модуля пока не опубликован преподавателем.</div>`}<div class="module-footer"><span class="module-state">${owner?'Преподаватель просматривает опубликованный материал':done?'✓ Завершение подтверждено преподавателем':'Ожидает подтверждения преподавателя'}</span></div></article>`}).join('')}
function scrollToHash(){if(location.hash){document.querySelector(location.hash)?.scrollIntoView({behavior:'smooth',block:'start'})}}
function lock(title,text){lockedTitle.textContent=title;lockedText.textContent=text;locked.classList.remove('hidden')}
out.onclick=async()=>{if(db)await db.auth.signOut();location.href='./'};init();